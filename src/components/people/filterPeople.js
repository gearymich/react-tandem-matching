import Peoples from './Peoples'

// TEMPORARY
const filterLocals = function(ftype, title) {
    console.log(ftype + ": "+ title + " EXEC FROM APP.js");
    // setPeoples(peoples.filter(
    //   (people) => people.hobby === title))
    }
export default filterLocals;
  